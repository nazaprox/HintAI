const Camera={stream:null,async openModal(){const m=document.getElementById('camera-modal'),v=document.getElementById('camera-video');if(!m||!v)return;m.classList.remove('hidden');try{this.stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'},audio:false});v.srcObject=this.stream}catch(e){UI.showToast('Accès caméra refusé.','error');this.closeModal()}},closeModal(){document.getElementById('camera-modal')?.classList.add('hidden');this.stream?.getTracks().forEach(t=>t.stop());this.stream=null},capturePhoto(){const v=document.getElementById('camera-video');if(!v||!this.stream)return;const c=document.createElement('canvas');c.width=v.videoWidth||640;c.height=v.videoHeight||480;c.getContext('2d').drawImage(v,0,0,c.width,c.height);c.toBlob(b=>{if(b)Uploads.processFile(new File([b],`photo_${Date.now()}.jpg`,{type:'image/jpeg'}));this.closeModal()},'image/jpeg',.9)}};window.Camera=Camera;
