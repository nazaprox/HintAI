const UI={activeTab:'tab-home',init(){document.querySelectorAll('.nav-item').forEach(n=>n.addEventListener('click',()=>this.switchTab(n.dataset.tab)));document.documentElement.dataset.theme=Storage.getTheme()},switchTab(id){this.activeTab=id;document.querySelectorAll('.tab-view').forEach(v=>v.classList.remove('active'));document.querySelectorAll('.nav-item').forEach(n=>n.classList.remove('active'));document.getElementById(id)?.classList.add('active');document.querySelector(`.nav-item[data-tab="${id}"]`)?.classList.add('active');window.scrollTo({top:0,behavior:'smooth'})},toggleTheme(){const t=Storage.getTheme()==='dark'?'light':'dark';Storage.setTheme(t);this.showToast(`Mode ${t==='dark'?'sombre':'clair'} activé`,'info')},showToast(message,type='info'){const c=document.getElementById('toast-container');if(!c)return;const e=document.createElement('div');e.textContent=message;e.style.cssText='background:var(--primary);color:#fff;padding:.7rem 1rem;border-radius:12px;margin:.25rem;box-shadow:0 8px 25px #0002';c.appendChild(e);setTimeout(()=>e.remove(),3000)},setLoading(v,text='Chargement...'){const o=document.getElementById('loading-overlay');if(!o)return;o.classList.toggle('hidden',!v);const t=document.getElementById('loading-text');if(t)t.textContent=text},async claimRewardAd(){try{this.setLoading(true,'Vérification...');const r=await API.claimReward();Auth.currentUser={...Auth.currentUser,credits:r.new_balance};Auth.updateUI();this.showToast(r.message,'success')}catch(e){this.showToast(e.message,'error')}finally{this.setLoading(false)}}};window.UI=UI;
